import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default function EditScreen({ navigation }) {
  console.log(navigation);

  return (
    <View>
      <Text>Edit Screen works!</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
